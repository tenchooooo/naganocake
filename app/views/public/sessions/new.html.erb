<div class="container">
  <h2>ログイン</h2>
  <p>会員の方はこちらからログイン</p>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div  class="row m-3">
          <%= f.label :email, "メールアドレス", class:'col-sm-3 form-inline' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'col-sm-6 form-inline'%>
        </div>
        <div class="row m-3">
          <%= f.label :password, "パスワード", class:'col-sm-3 form-inline' %>
          <%= f.password_field :password, autocomplete: "current-password", class:'col-sm-6 form-inline' %>
        </div>
        <div class="row m-3">
          <%= f.submit "ログイン",class:'col-sm-2 offset-sm-3 mt-3 btn btn-primary' %>
        </div>
      <% end %>
      <%= render "admin/shared/links" %>
    </div>
  <% end %>
  <p>会員登録がお済みでない方は</p>
  <p><span><%= render "public/shared/links" %></span>から新規登録を行ってください。</p>
</div>

